function rectangle(a, b ){
    const perimeter = 2 * (a + b)
    const area = a * b
    const Object = {
        length: a,
        width: b, 
        perimeter: perimeter,
        area: area
    }
    return Object

    

}